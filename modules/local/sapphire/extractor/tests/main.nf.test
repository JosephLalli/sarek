nextflow_process {

    name "Test Process SAPPHIRE_EXTRACTOR"
    script "../main.nf"
    process "SAPPHIRE_EXTRACTOR"

    tag "modules"
    tag "modules_local"
    tag "sapphire"
    tag "sapphire_extractor"

    test("human - stub") {
        options "-stub"
        when {
            process {
                """
                input[0] = [
                    [ id:'test' ], // meta
                    file("test.vcf.gz"),
                    file("test.vcf.gz.tbi"),
                    'chr21'
                ]
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
