nextflow_process {

    name "Test Process VG_CONVERT"
    script "../main.nf"
    process "VG_CONVERT"

    tag "modules"
    tag "modules_local"
    tag "vg"
    tag "vg_convert"

    test("Convert graph format (Stub)") {
        options "-stub"
        when {
            process {
                """
                input[0] = [ [id:'test'], file("test.gbz") ]
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }
    }
}
