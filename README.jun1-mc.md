# hprc-jun1-mc=* Information

## Cactus commit: 91bdd83728c8cdef8c34243f0a52b28d85711bcf (equivalent to Release 2.1.0)

These are exactly the instructions from the documentation (but using on-demand instead of spot), flattened into a single line to be pasted into a AWS head node:

https://github.com/ComparativeGenomicsToolkit/cactus/blob/91bdd83728c8cdef8c34243f0a52b28d85711bcf/doc/pangenome.md#hprc-graph

* Files with "-full" contain all contigs that could be assigned a chromosome, nothing masked or clipped.
* Files with "-min.af.0.1" were clipped to keep only nodes covered by ?= 10% (9) haplotypes (only suitable for short-read mapping)
* Files with neither suffix were clipped so that path intervals in the graph that span >=10kb without aligning to the minigraph are removed (this procedure stands in the for the complicated multi-step (including dna-brnn) procedure used in v1.0).

Total wall time to produce all outputs (not including minigraph construction): ~20 hours. 

### GRCh38-based graph

export MYJOBSTORE=aws:us-west-2:glennhickey-jobstore77
export VERSION=jun1
export MYBUCKET=s3://vg-k8s/vgamb/wg/cactus/GRCh38-f1g-90/jun1
export MINIGRAPH=s3://vg-k8s/vgamb/wg/cactus/GRCh38-f1g-90/jun1/GRCh38-90c.r518.gfa.gz

cactus-graphmap ${MYJOBSTORE} hprc-${VERSION}-mc.pp.seqfile ${MINIGRAPH} ${MYBUCKET}/hprc-${VERSION}-mc-grch38.paf --outputGAFDir ${MYBUCKET}/gaf-hprc-${VERSION}-mc-grch38 --outputFasta ${MYBUCKET}/fasta/minigraph.grch38.gfa.fa.gz --reference GRCh38 --mapCores 16 --delFilter 10000000 --realTimeLogging --batchSystem mesos --provisioner aws --defaultPreemptable --nodeType r5.8xlarge --nodeStorage 650 --maxNodes 25 --betaInertia 0 --targetTime 1  --logFile hprc-${VERSION}-mc-grch38.paf.log    &&    cactus-graphmap-split ${MYJOBSTORE}  hprc-${VERSION}-mc.pp.seqfile ${MINIGRAPH} ${MYBUCKET}/hprc-${VERSION}-mc-grch38.paf --outDir ${MYBUCKET}/chroms-hprc-${VERSION}-mc-grch38 --otherContig chrOther --refContigs $(for i in `seq 22`; do echo chr$i; done ; echo "chrX chrY chrM") --reference GRCh38 --realTimeLogging --batchSystem mesos --provisioner aws --defaultPreemptable --nodeType r5.8xlarge --nodeStorage 1000 --maxNodes 5 --betaInertia 0 --targetTime 1 --logFile hprc-${VERSION}-mc-grch38.split.log   &&   aws s3 cp ${MYBUCKET}/chroms-hprc-${VERSION}-mc-grch38/chromfile.txt .   &&   cactus-align-batch ${MYJOBSTORE} ./chromfile.txt ${MYBUCKET}/align-hprc-${VERSION}-mc-grch38 --alignCores 16 --realTimeLogging --alignOptions "--pangenome --pafInput --maxLen 10000 --reference GRCh38 --realTimeLogging  --outVG" --batchSystem mesos --provisioner aws --defaultPreemptable --nodeType r5.8xlarge --nodeStorage 1000 --maxNodes 20 --betaInertia 0 --targetTime 1 --logFile hprc-${VERSION}-mc-grch38.align.log   &&   cactus-graphmap-join ${MYJOBSTORE} --vg $(for j in $(for i in `seq 22`; do echo chr$i; done ; echo "chrX chrY chrM chrOther"); do echo ${MYBUCKET}/align-hprc-${VERSION}-mc-grch38/${j}.vg; done) --hal $(for j in $(for i in `seq 22`; do echo chr$i; done ; echo "chrX chrY chrM chrOther"); do echo ${MYBUCKET}/align-hprc-${VERSION}-mc-grch38/${j}.hal; done) --outDir ${MYBUCKET}/ --outName hprc-${VERSION}-mc-grch38-full --reference GRCh38 --gfaffix  --wlineSep "."  --rename "CHM13v2>CHM13v2.0" --batchSystem mesos --provisioner aws --defaultPreemptable --nodeType r5.16xlarge --nodeStorage 1000 --maxNodes 1 --indexCores 63 --realTimeLogging --logFile hprc-${VERSION}-mc-grch38-full.join.log   &&   cactus-graphmap-join ${MYJOBSTORE} --vg $(for j in $(for i in `seq 22`; do echo chr$i; done ; echo "chrX chrY chrM chrOther"); do echo ${MYBUCKET}/clip-hprc-${VERSION}-mc-grch38-full/${j}.vg; done) --outDir ${MYBUCKET}/ --outName hprc-${VERSION}-mc-grch38 --reference GRCh38  --wlineSep "." --clipLength 10000 --clipNonMinigraph --vcf --giraffe --preserveIDs --batchSystem mesos --provisioner aws --defaultPreemptable --nodeType r5.16xlarge --nodeStorage 1000 --maxNodes 2 --indexCores 64 --betaInertia 0 --targetTime 1 --realTimeLogging --logFile hprc-${VERSION}-mc-grch38.join.log    &&   cactus-graphmap-join ${MYJOBSTORE} --vg $(for j in $(for i in `seq 22`; do echo chr$i; done ; echo "chrX chrY chrM chrOther"); do echo ${MYBUCKET}/clip-hprc-${VERSION}-mc-grch38/${j}.vg; done) --outDir ${MYBUCKET} --outName hprc-${VERSION}-mc-grch38-minaf.0.1 --reference GRCh38  --wlineSep "." --vgClipOpts "-d 9 -m 1000" --preserveIDs --giraffe --batchSystem mesos --provisioner aws --defaultPreemptable --nodeType r5.16xlarge --nodeStorage 1000 --maxNodes 2 --indexCores 64 --realTimeLogging --logFile hprc-${VERSION}-mc-grch38-minaf.0.1.join.log

### CHM13-based graph

export MYJOBSTORE=aws:us-west-2:glennhickey-jobstore78
export VERSION=jun1
export MYBUCKET=s3://vg-k8s/vgamb/wg/cactus/GRCh38-f1g-90/jun1
export MINIGRAPH=s3://vg-k8s/vgamb/wg/cactus/GRCh38-f1g-90/jun1/chm13-90c.r518.gfa.gz

cactus-graphmap ${MYJOBSTORE} hprc-${VERSION}-mc.pp.seqfile ${MINIGRAPH} ${MYBUCKET}/hprc-${VERSION}-mc-chm13.paf --outputGAFDir ${MYBUCKET}/gaf-hprc-${VERSION}-mc-chm13 --outputFasta ${MYBUCKET}/fasta/minigraph.grch38.gfa.fa.gz --reference CHM13v2 --mapCores 16 --delFilter 10000000 --realTimeLogging --batchSystem mesos --provisioner aws --defaultPreemptable --nodeType r5.8xlarge --nodeStorage 650 --maxNodes 25 --betaInertia 0 --targetTime 1  --logFile hprc-${VERSION}-mc-chm13.paf.log    &&    cactus-graphmap-split ${MYJOBSTORE}  hprc-${VERSION}-mc.pp.seqfile ${MINIGRAPH} ${MYBUCKET}/hprc-${VERSION}-mc-chm13.paf --outDir ${MYBUCKET}/chroms-hprc-${VERSION}-mc-chm13 --refContigs $(for i in `seq 22`; do echo chr$i; done ; echo "chrX chrY chrM") --reference CHM13v2 --realTimeLogging --batchSystem mesos --provisioner aws --defaultPreemptable --nodeType r5.8xlarge --nodeStorage 1000 --maxNodes 5 --betaInertia 0 --targetTime 1 --logFile hprc-${VERSION}-mc-chm13.split.log   &&   aws s3 cp ${MYBUCKET}/chroms-hprc-${VERSION}-mc-chm13/chromfile.txt .   &&   cactus-align-batch ${MYJOBSTORE} ./chromfile.txt ${MYBUCKET}/align-hprc-${VERSION}-mc-chm13 --alignCores 16 --realTimeLogging --alignOptions "--pangenome --pafInput --maxLen 10000 --reference CHM13v2 --realTimeLogging  --outVG" --batchSystem mesos --provisioner aws --defaultPreemptable --nodeType r5.8xlarge --nodeStorage 1000 --maxNodes 20 --betaInertia 0 --targetTime 1 --logFile hprc-${VERSION}-mc-chm13.align.log   &&   cactus-graphmap-join ${MYJOBSTORE} --vg $(for j in $(for i in `seq 22`; do echo chr$i; done ; echo "chrX chrY chrM"); do echo ${MYBUCKET}/align-hprc-${VERSION}-mc-chm13/${j}.vg; done) --hal $(for j in $(for i in `seq 22`; do echo chr$i; done ; echo "chrX chrY chrM"); do echo ${MYBUCKET}/align-hprc-${VERSION}-mc-chm13/${j}.hal; done) --outDir ${MYBUCKET}/ --outName hprc-${VERSION}-mc-chm13-full --reference CHM13v2 --gfaffix --xgReference GRCh38 --wlineSep "."  --rename "GRCh38>GRCh38.0" --batchSystem mesos --provisioner aws --defaultPreemptable --nodeType r5.16xlarge --nodeStorage 1000 --maxNodes 1 --indexCores 63 --realTimeLogging --logFile hprc-${VERSION}-mc-chm13-full.join.log   &&   cactus-graphmap-join ${MYJOBSTORE} --vg $(for j in $(for i in `seq 22`; do echo chr$i; done ; echo "chrX chrY chrM"); do echo ${MYBUCKET}/clip-hprc-${VERSION}-mc-chm13-full/${j}.vg; done) --outDir ${MYBUCKET}/ --outName hprc-${VERSION}-mc-chm13 --reference CHM13v2  --wlineSep "." --clipLength 10000 --clipNonMinigraph --vcf --vcfReference GRCh38 --xgReference GRCh38 --giraffe --preserveIDs --batchSystem mesos --provisioner aws --defaultPreemptable --nodeType r5.16xlarge --nodeStorage 1000 --maxNodes 2 --indexCores 64 --betaInertia 0 --targetTime 1 --realTimeLogging --logFile hprc-${VERSION}-mc-chm13.join.log    &&   cactus-graphmap-join ${MYJOBSTORE} --vg $(for j in $(for i in `seq 22`; do echo chr$i; done ; echo "chrX chrY chrM"); do echo ${MYBUCKET}/clip-hprc-${VERSION}-mc-chm13/${j}.vg; done) --outDir ${MYBUCKET} --outName hprc-${VERSION}-mc-chm13-minaf.0.1 --reference CHM13v2  --wlineSep "." --vgClipOpts "-d 9 -m 1000 -P GRCh38" --xgReference GRCh38 --preserveIDs --giraffe --batchSystem mesos --provisioner aws --defaultPreemptable --nodeType r5.16xlarge --nodeStorage 1000 --maxNodes 2 --indexCores 64 --realTimeLogging --logFile hprc-${VERSION}-mc-chm13-minaf.0.1.join.log
